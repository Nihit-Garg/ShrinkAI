fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0          # Required for pydantic EmailStr

# Database
sqlalchemy>=2.0.0
alembic>=1.14.0
psycopg2-binary>=2.9.0          # PostgreSQL driver

# Supabase (Auth only)
supabase>=2.10.0

# Vector store
chromadb>=0.5.0

# LLMs
google-genai>=0.8.0             # Gemini Flash for completions
groq>=0.13.0                    # Groq fallback (Llama 3.3 70B)
# Embeddings: nomic-embed-text via Ollama (local) — no API key needed
# Run: ollama pull nomic-embed-text

# RAG framework (Week 2)
llama-index>=0.12.0
llama-index-vector-stores-chroma>=0.4.0

# Hybrid retrieval (Week 3)
rank-bm25>=0.2.2
sentence-transformers>=3.0.0    # Cross-encoder reranker

# Evaluation (Week 3)
ragas>=0.2.0

# HTTP
httpx>=0.27.0
python-multipart>=0.0.9
